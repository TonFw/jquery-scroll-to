require 'spec_helper'

describe Scrolltojs::Rails do
  it 'has a version number' do
    expect(Scrolltojs::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
